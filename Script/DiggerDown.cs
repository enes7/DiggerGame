using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class DiggerDown : MonoBehaviour
{
    

    //Digger ın aşağı inmesi için ayrı bir yol

    /*#region Region Down
    public void OnPointerDown(PointerEventData data)
    {
        Joystickmove.instance.down = true;
    }
    public void OnPointerUp(PointerEventData data)
    {
        Diggersc.intance.Startposition();
        Joystickmove.instance.down = false;
        //Diggersc.intance.Digger.transform.Translate(0, 0, 0 * Time.deltaTime);
    }
    #endregion
    */
    
}
